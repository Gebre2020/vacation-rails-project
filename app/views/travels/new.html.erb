<h1>New Travel Form</h1>

<%= form_for(@travel) do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :address %>
  <%= f.text_field :address %>

   <%= f.submit "Create Travel" %>
<% end %>